{
  "name": "bio-baum-bauer",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start:dev": "docker-compose -f docker-compose.yml up -d --build",
    "stop:dev": "docker-compose -f docker-compose.yml down"
  }
}
